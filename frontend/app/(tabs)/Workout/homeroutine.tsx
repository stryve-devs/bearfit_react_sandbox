import React from 'react';
// Going up: 1. Workout -> 2. (tabs) -> 3. app -> root
// Then down: src/screens/Workout/HomeRoutineScreen
import HomeRoutineScreen from '../../../src/screens/Workout/HomeRoutineScreen';

export default function HomeRoutinePage() {
    return <HomeRoutineScreen />;
}